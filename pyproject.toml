[project]
name = "kube-incident"
version = "0.1.0"
description = ""
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.141.1",
    "pydantic>=2.13.4",
    "pytest>=9.1.1",
    "uvicorn>=0.52.4",
    "httpx2",
    "sqlmodel>=0.0.42",
    "sqlalchemy",
    "python-dotenv>=1.2.3",
    "pydantic-settings>=2.15.0",
    "psycopg2-binary"
]
authors = {name = "Christian Arroyo"}

[tool.fastapi]
entrypoint = "apps.checkout_api.main:app"

[tool.pytest.ini_options]
pythonpath = "."
